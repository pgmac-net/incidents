# Public build -> https://incidents.pgmac.net.au/ (GitHub Pages)
#
# Everything except the target-specific settings below lives in mkdocs-base.yml.

INHERIT: mkdocs-base.yml

site_url: https://incidents.pgmac.net.au/
site_dir: docs/
